[package]
name = "mirae-engine"
description = "Minimal engine process entry point."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
# An application assembles libraries and holds no domain logic (801 section 4).
mirae-contracts = { path = "../../crates/foundation/contracts" }
mirae-observability = { path = "../../crates/foundation/observability" }
mirae-runtime = { path = "../../crates/runtime/runtime" }

[lints]
workspace = true
