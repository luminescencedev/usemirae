[package]
name = "mirae-domain"
description = "Project-domain entities and validation. No OS, GPU, FFmpeg, UI, or network dependencies."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
# Identity only. 804 section 2: the domain sits directly above the foundation
# and reaches nothing else.
mirae-types = { path = "../../foundation/types" }

[lints]
workspace = true
