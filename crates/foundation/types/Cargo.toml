[package]
name = "mirae-types"
description = "Stable IDs, generations, rational time, bounded primitive wrappers, and common enums."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
# The foundation layer may depend on nothing above it (804 section 2). These two
# are format and identity only: no runtime, no platform, no UI.
serde = { workspace = true }
uuid = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }

[lints]
workspace = true
