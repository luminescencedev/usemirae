[package]
name = "mirae-media-ffmpeg"
description = "Contained FFmpeg adapters. The only crate allowed to depend on FFmpeg."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish = false

[lints]
workspace = true
