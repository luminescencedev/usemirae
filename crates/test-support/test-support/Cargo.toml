[package]
name = "mirae-test-support"
description = "Shared test fixtures, fake clocks, and deterministic transports."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
# The harness speaks the real protocol, so it uses the real codec and contracts.
mirae-contracts = { path = "../../foundation/contracts" }
mirae-runtime = { path = "../../runtime/runtime" }
serde_json = { workspace = true }

[lints]
workspace = true
