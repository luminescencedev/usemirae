# fixtures/

Shared test fixtures used across workspaces.

Rules:

- large binary fixtures are minimized, documented, and hash-pinned;
- every fixture records what it proves and which ticket introduced it;
- no production media, credentials, or customer content.

Created empty by `MIR-0001`.
