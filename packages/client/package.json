{
  "name": "@mirae/client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Typed engine client, reconnect logic, and command/query/event abstractions.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@mirae/contracts": "workspace:*"
  },
  "devDependencies": {
    "@mirae/config": "workspace:*",
    "@mirae/test-utils": "workspace:*",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
