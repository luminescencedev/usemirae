{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "mirae://ipc/v1/bridge-response",
  "title": "BridgeResponse",
  "description": "The shell's answer to a control-UI request. It carries what the UI needs in order to show engine state honestly, including the case where the shell knows nothing because the engine is gone (501 section 10).",
  "type": "object",
  "properties": {
    "requestId": {
      "type": "string",
      "maxLength": 64,
      "description": "The request this answers, echoed back verbatim."
    },
    "ok": {
      "type": "boolean",
      "description": "Whether the request was answered. False carries an error code and no payload."
    },
    "errorCode": {
      "type": "string",
      "maxLength": 64,
      "description": "Why the request was refused, as a stable category rather than a message. Empty when ok."
    },
    "engineConnected": {
      "type": "boolean",
      "description": "Whether the shell currently holds an authenticated engine connection. False is reported as false, never as stale detail: 501 section 6 forbids the shell fabricating engine state while disconnected."
    },
    "engineSessionId": {
      "type": "string",
      "maxLength": 64,
      "description": "The engine session, when connected. Empty otherwise, so a client cannot mistake a previous session for the current one."
    },
    "protocolMajor": {
      "type": "integer",
      "minimum": 0,
      "maximum": 65535,
      "description": "Negotiated protocol major version, or zero when not connected."
    },
    "protocolMinor": {
      "type": "integer",
      "minimum": 0,
      "maximum": 65535,
      "description": "Negotiated protocol minor version, or zero when not connected."
    },
    "stateGeneration": {
      "type": "integer",
      "minimum": 0,
      "maximum": 18446744073709551615,
      "description": "The committed state generation the shell last observed, or zero when it has observed none."
    }
  },
  "required": [
    "requestId",
    "ok",
    "errorCode",
    "engineConnected",
    "engineSessionId",
    "protocolMajor",
    "protocolMinor",
    "stateGeneration"
  ],
  "additionalProperties": false
}
