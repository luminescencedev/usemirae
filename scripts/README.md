# scripts/

Thin entry points only. Scripts delegate to `cargo xtask`; they do not
reimplement build, generation, or test logic.

Created empty by `MIR-0001`.

Canonical documentation: `docs/08-development/806-build-system-and-toolchain.md` §4.
